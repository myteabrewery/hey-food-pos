# Hey Food — Build Status

Living document, updated after each milestone lands. Last updated: this session.

---

## Foundation (packages/)

| Package | Status |
|---|---|
| shared-types | ✅ Done — all entities, OrderStatus, StatusMeta |
| design-tokens | ✅ Done — v2 palette (navy/teal/yellow/cream), typography, spacing, radius. `OUTLET_HEALTH_COLORS` (`good`/`warning`/`critical`) added for HQ Admin's outlet-health indicator, documented in docs/hey-food-design-system-v1.md Section 2 alongside `ORDER_STATUS_META` — see HQ Admin section below for why `warning` needed a genuinely new color, not a reuse of brand `yellow`. `DANGER_COLORS` (`solid`/`tint`) added for destructive actions (Cancel order), documented in the design system Section 2 — an independent token with its own literal values, intentionally **not** derived from `ORDER_STATUS_META.cancelled`, so a change to the Cancelled status color or to the danger red can never silently change the other; proposed values pending designer confirmation. |
| api-client | ✅ Done — zod schemas for all dev spec endpoints except GET /admin/reports (deferred) |
| eslint-config | ✅ Done — base + react-native + next configs |
| tsconfig | ✅ Done (scaffolding) |

## Backend (apps/hey-food-backend)

| Piece | Status |
|---|---|
| Database (Postgres + Prisma schema + migration + seed) | ✅ Done |
| GET /outlets/nearby | ✅ Done, verified against real seeded data |
| GET /outlets/:id (resolved menu) | ✅ Done, verified against real seeded data |
| `ProductModifierOption.imageUrl` (nullable) | ✅ Done — shared-types/api-client/Prisma migration (additive, no backfill) all updated, `outlets.service.ts` wired through, seed-soup-stall.ts seeded with `null` for every option. Now consumed by the Customer App's ingredient photo grid (see below) — falls back to a lettered placeholder tile until real photos are added by editing the seed data. |
| Auth (OTP request/verify) | 🔲 Not started — next up |
| POST /orders (order creation) | 🟡 **Shared order-creation service is built** (`src/orders/order-creation.service.ts`, used by `POST /guest/orders` — see below); the JWT-authenticated `POST /orders` itself is still blocked on auth and will call the same service. |
| `PATCH /pos/orders/:id/status`, `POST /pos/orders/:id/cancel` (POS Stage B) | ✅ **Done, verified over real HTTP and on a real phone.** Behind the same temporary device key as the read endpoint. **State machine enforced server-side** (dev spec §3, "no state can be skipped"): `received→preparing→ready→collected`, each with exactly one legal source and its own timestamp; a skip or backwards move is **409 `INVALID_STATUS_TRANSITION`** with a message naming the current status. A Collect writes `collected_at` and `completed_at` together (`collected→completed` is automatic and immediate, so the stored status is `completed`). **Repeats are silent no-ops** (200, timestamp not re-stamped) per §10 — a retry after a lost response succeeds instead of making the tablet roll back; every write is a conditional `UPDATE … WHERE status = <expected>`, so two devices racing, or a cancel landing mid-Start, can't both apply. **Cancel:** staff only (`actor` `customer`/`hq` → 400), four typed reasons, free-text detail only for `other` (trimmed, blank → NULL, ≤ 200 chars; a stray detail on another reason is dropped so the `orders_cancel_detail_only_for_other` CHECK can't be violated), allowed from any status except `completed`/`cancelled`, an identical repeat is a no-op, a *different* reason on an already-cancelled order is 409 (never silently rewritten). Cancelling an order that had been paid logs `[REFUND NOT IMPLEMENTED]`. **Verified:** 46 automated checks over HTTP (auth, every invalid transition, idempotency, all cancel variants, 6 parallel Starts, 12 randomized Start-vs-Cancel races), plus the on-device run under the POS Stage B row. |
| Billplz payment integration | 🔲 Not started — **stubbed**: `POST /orders/:id/pay` uses an env-gated stand-in that marks orders paid WITHOUT payment. See the temporary-stand-ins row below. |
| `Order.cancelReasonDetail` (nullable) — ✅ **Done** | Cancellation data kept separable for reporting: `cancel_reason` stays the clean enum value (`item_unavailable`/`customer_no_show`/`kitchen_error`/`other`), and the new `cancel_reason_detail` holds free text **only when the reason is `other`** — same separation as `OrderItemModifier`'s snapshot fields rather than a concatenated string. Added to shared-types `Order`, api-client `OrderSchema`, Prisma (additive migration `add_order_cancel_reason_detail`, applied to the dev DB) and dev spec Sections 1/3. The migration also adds a hand-written CHECK, `orders_cancel_detail_only_for_other` (beyond the letter of the request — cheap to drop if unwanted): detail must be NULL unless `cancel_reason` is `'other'`. **Verified on the live DB** (9 cases: never-cancelled, each non-other reason, other with/without detail accepted; detail on a non-other reason and detail with no reason at all rejected, pg 23514) — and that test caught a real bug in the first draft of the constraint: `cancel_reason = 'other'` is NULL (not false) when the reason is NULL, and a CHECK treats NULL as passing, so a detail with no reason slipped through; fixed with an explicit `IS NOT NULL` (the migration was undone and re-applied cleanly, `migrate status` reports no drift). Because `Order` gained a required field, the two hand-built mock orders (POS `mock/orders.ts`, Customer App `api/orders.ts` — the latter is parsed against the schema, so it would have thrown at runtime) now carry `cancelReasonDetail: null`. |
| Guest-order identity (nullable `orders.customer_id`, `guest_phone`, `guest_token_hash`, `orders_identity_xor` CHECK) | ✅ Schema done and verified on the live DB; `POST /guest/orders` now uses it — see "Guest Web Checkout" below. |
| Order display IDs — outlet prefix + daily counter | ✅ **Done.** Migration `20260920100000_add_display_prefix_daily_counter_idempotency`: `Outlet.displayPrefix` (unique per business; seeded PM/KC/MV/SS), `Order.businessDate` + `dailySeq`, and an `order_daily_counters` table replace the old **globally unique** `display_id` (which contradicted dev spec §3's "PM001, PM002… resetting at midnight"). Uniqueness is now per (outlet, business day, display_id). The number comes from one atomic `INSERT … ON CONFLICT DO UPDATE … RETURNING` inside the order transaction. Hand-edited to add-nullable → backfill → `SET NOT NULL` so it applies to a populated DB (backfilled the existing PM042 as seq 42). **Verified:** 20 concurrent creations → 20 distinct consecutive IDs; a failed insert rolls its number back; resets per Malaysian calendar day. Business day is fixed to `Asia/Kuala_Lumpur`. |
| `POST /guest/orders`, `GET /orders/:id`, `POST /orders/:id/pay` (guest-token access) | ✅ **Done, verified end to end** (see Guest Web Checkout Stage 5). Prices computed server-side in integer cents from the outlet-resolved menu (`src/menu/outlet-pricing.ts`, now also used by `GET /outlets/:id`, so displayed and charged prices share one implementation) + `validateSelectedModifiers`. Rejects: closed outlet (409), sold-out product (409), unknown product / bad modifiers (400), quantity > 20, > 30 lines, notes > 200 chars. Guest token = 32 random bytes, only its SHA-256 stored, sent as `Authorization: Bearer` and compared in constant time; a wrong token and a nonexistent order are the same 404. `Idempotency-Key` header: same key + same body returns the same order with a **rotated** token (the original can't be re-issued; the old one stops working), same key + different body → 422. Rate limit 5/min per IP on create (10 pay, 60 read; per-controller, not global — the web app's SSR menu fetches share one IP). CORS allow-list is exactly `WEB_ORIGIN`; `X-Pos-Device-Key` is deliberately not an allowed header. |
| ⚠️ **TEMPORARY stand-ins (must be replaced before launch)** | (1) **Payment stub** — `PAYMENT_STUB_ENABLED=true`: `POST /orders/:id/pay` marks the order paid with no payment, no `Payment` row (stub-paid orders = `status paid`, `payment_id NULL`), a `[STUB PAYMENT]` warning per use, `isStub: true` in the response (the web app shows a "TEST MODE" banner); off → 501. (2) **POS device key** — `POS_DEVICE_KEY` shared secret, `X-Pos-Device-Key` header; fails closed (503) if unset; **not per-device/outlet/revocable and it ships inside the POS bundle, so it is not secret. Since Stage B it guards WRITES: anyone with the key can move or cancel any outlet's orders, and nothing records who did.** (3) **Service fee** — flat RM2.00 placeholder (`SERVICE_FEE_CENTS`), not a decided business rule. (4) **Push stub** — `PUSH_STUB_ENABLED=true`: push "notifications" are only logged, nothing is pushed (and there is nothing to push to yet: no device-token registry, no push code in the Customer App). (5) **SMS stub** — `SMS_STUB_ENABLED=true`: SMS is only logged, nothing is texted (no provider is chosen). **With the notification stubs `orders.notified_at` IS set, but that is not proof anyone was told:** the tell is `notification_logs.payload` → `provider = logging-stub`, `stub = true`, and `delivered = false` (which is `false` on every row today). A production process **refuses to start** with (1), (2), (4) or (5) set (verified for each notification flag on its own, and a clean production config still starts). Full list, including the gaps Stage B creates (refunds, customer notifications, offline queue): backend README "Pre-launch checklist". |
| ⚠️ **No audit trail for who changed an order** | A status change or cancel records **when** (`preparing_at`, `ready_at`, `collected_at`/`completed_at`, `cancelled_at`) and, for a cancel, **why** (`cancel_reason` / `cancel_reason_detail`) — but **not who, and not from which device**. No staff, device or actor id is stored on the order or in any event table, and the backend logs no per-request record of these calls (its only log line is the refund warning). The `actor: "staff"` field on a cancel is a fixed category the client asserts, not an identity. Because the device key is shared, even server access logs could not tell one tablet or person from another. So "who cancelled PM002?" is unanswerable today, and a wrongful cancel, a fat-fingered Collect, or misuse of an extracted key cannot be attributed. Sits with the device-key note above as one launch blocker: real staff auth (staff id + device id) first, then record it on every transition (actor columns or an `order_events` table). |
| `GET /pos/outlets/:outletId/orders` (POS queue) | ✅ **Done.** Returns `received` / `preparing` / `ready` orders, oldest first; unpaid `pending` orders are excluded. **It is also the POS "sync" of dev spec §3: every `paid` order for the outlet becomes `received` (`received_at` stamped) the moment a POS fetches the queue** — the one write on a GET, idempotent. Chosen because §3 says `paid→received` is "automatic on successful sync, not a staff action"; an order the POS couldn't reach correctly stays `paid`. (Verified: an order paid at 07:30:09 was `received` at 07:30:12, stamped by the phone's poll.) Behind the temporary device key. |
| ⚠️ **Side effect: the POS queue GET writes** | `GET /pos/outlets/:outletId/orders` is **not read-only** — fetching it moves every `paid` order for that outlet to `received` and stamps `received_at` (idempotent; deliberate, because dev spec §3 makes `paid→received` "automatic on successful sync"). Consequences: any caller holding the device key triggers it, so a curl, a second tablet or a monitoring probe all count as "the POS received it"; and no proxy/CDN/prefetcher may cache or replay this GET. |
| Notifications module — provider interfaces + logging stubs (`src/notifications/`) | ✅ **Built.** `SmsProvider.send(toE164, body)` and `PushProvider.send(customerId, message)` are **two interfaces, not one**: they address and fail differently, and the SMS one is what the OTP login will reuse. Push is addressed by **customer id, not a device token**, because no token registry exists (no table, no registration endpoint, and the Customer App has no push code); a real provider will own the token lookup. `ProviderResult.accepted` means the provider took the message, **not** that a phone received it. Both providers today are `logging-stub`s that **send nothing**, each behind its own flag (`PUSH_STUB_ENABLED` / `SMS_STUB_ENABLED`, see the stand-ins row); with a flag off the provider is `null` and the attempt is recorded as failed ("no … provider configured"), never skipped. Dev-only fault injection (`PUSH_STUB_FAIL` / `SMS_STUB_FAIL` = `throw` / `reject` / `slow` / `hang`) and a per-provider timeout (`NOTIFY_PROVIDER_TIMEOUT_MS`, default 10 s) exist so the failure paths can be exercised for real. `NotificationsService.notifyOrderReady(order)` never throws; every attempt writes one `notification_logs` row (`event`, `provider`, `stub`, masked `to`, message, `accepted`, `error`); phone numbers are masked (`+6012***6789`) in logs and payloads. The event type lives in the row's JSON payload, not a column (`NotificationLog` has none). The row the seed wrote for PM042 ("Order received.") is an older, differently shaped payload with no `event`. |
| Ready notification (dev spec §6), wired into `PATCH /pos/orders/:id/status` | ✅ **Done and verified; the providers are STUBS, so no customer is actually notified.** Fires from the request that **wins** the `preparing→ready` update and nowhere else: a repeated Ready (a silent no-op), a request that lost a race, and every other transition send nothing. **App-based customer → push AND SMS in parallel** (spec §6: SMS is not a fallback); **guest → SMS only**, to `guest_phone`. Text per spec: `#PM001 is ready! Collect from Hey Food — Paradigm Mall.` It runs **after the status change commits and is not awaited**, so the staff's Ready tap never waits on a provider (the response carries `notifiedAt: null`; the log row lands afterwards). `notified_at` is set **only if a provider accepted** (one channel failing does not hide the other). **Verified — (1) Guest, fully real:** an order placed through the real web checkout in Chrome and paid, then moved Start → Ready through the POS endpoints: exactly one `sms` row (`logging-stub`, `stub true`, `delivered false`, `to +6019***0142`), **no push row**, `notified_at` set, the server logged `[STUB SMS] NOT SENT … would text +6019***0142`, two repeated Readys added nothing, and the guest's status page (unchanged) showed "Ready for pickup". **The Ready call was sent as the same HTTP request the POS makes (curl with the device key), NOT tapped on the phone: no device was connected for this pass.** The phone's Ready tap was verified against Stage B earlier and this pass changed nothing in the POS app. **(2) App-based — SYNTHETIC, not a real app order:** no app-based order can be placed today (`POST /orders` and auth are not built; the only app-based data is the seeded PM042 / `customer_mock`), so these orders were created through the shared order service with `identity: customer` and then driven only through the real POS endpoints. Result: one `push` row (addressed `customer:customer_mock`) plus one `sms` row to the customer's phone (`+6012***6789`), both accepted, `notified_at` set; 6 parallel Readys still produced exactly one pair. **The push path can only be tested with synthetic/seeded data until app ordering exists, and a real push additionally needs the token registry and Customer App push code.** **(3) Failure paths, real, via the stubs' fault injection:** provider throws → Ready still 200, failed row carrying the error, `notified_at` NULL; provider rejects → same; app order with SMS throwing → push row accepted + sms row failed, `notified_at` SET; provider takes 3 s → Ready returned in 37 ms, no row until the provider answered, then accepted; provider never answers → Ready in 31 ms and the 1.5 s timeout closed the attempt as failed; both flags off → failed rows "no … provider configured", never silently skipped. **Known gaps:** no retry / outbox (a crash or outage after Ready means that customer is never told; the failed rows are how to find them), no delivery receipts (`delivered` is always false), only `ready` notifies (the `paid` and `preparing` pushes of §6 are not built), English only, and nothing real is sent until real providers exist. |
| ⚠️ **Known gap: a lost response after a successful action shows a false failure** | If the server applies a staff action but the response never reaches the tablet (a dropped connection, or a timeout on the way back), the POS cannot tell that from a failed request: it **rolls the card back and shows the red "Couldn't … It has been put back as it was" banner even though the order changed on the server.** For **Ready** this now has a customer-visible edge: the order IS ready and the customer's notification HAS already fired, while the staff screen says it failed. **It self-corrects:** the next poll (~5 s) puts the card where the server says it is, and the banner clears itself after 10 s or on tap. (If a poll that was already in flight predates the action it can take one further cycle; that is reasoned from `useLiveOrders`, not exercised on a device.) **Retrying is safe:** a repeated Ready / Start / Collect or an identical Cancel is a silent no-op on the server, and a repeated Ready sends **no second notification** (verified over HTTP: two repeat Readys left exactly one log row). **Not fixed in this pass.** What Stage B's on-device rollback tests did cover: a request that never reached the server (backend stopped) and a 409; a lost *response* was not tested. This is the same underlying gap as the missing durable outbox/retry: the space between "the server did the thing" and "the client knows the server did the thing". Revisit them together (and with the offline action queue, which needs the same notion of an outcome that is not yet known). |
| GET /admin/reports | 🔲 Deferred — needs its own design pass |
| Known runtime caveat | ts-node runtime is a confirmed **stopgap**, not a production decision — see backend README |

## Customer App (apps/hey-food-customer)

| Screen/Feature | Status |
|---|---|
| Home (composite: hero, categories, outlet card, menu preview, order-progress) | ✅ Done, real data wired |
| Menu (item list, sold-out treatment) | ✅ Done, real data wired |
| Product Detail (customization/add-ons) | ✅ V1 (docs/product-customization-v1.md) all 3 stages done, tested on-device against real seeded Chicken Rice data. Superseded mid-flight by V2 (docs/product-customization-v2.md: `required` → `minSelections`/`maxSelections`, per-option `quantityEnabled`/`quantity`) — V2 Stage 1 (shared-types/api-client schemas) and Stage 2 (Prisma migration, rewritten validateSelectedModifiers, two named seed profiles — placeholder + the real soup-stall menu) done and verified. ✅ Stage 3.5 (rebuilding ModifierGroupSelector/ProductDetailScreen/CartSheetScreen against V2's real shape — real minSelections/maxSelections status text, quantity steppers on `quantityEnabled` options, per-option quantity flowing through to the cart) done and verified on-device against the real soup-stall seed data (DIY Soup Bowl: blocked at 1 ingredient, unlocked at 2 distinct ingredients, stepper live-updates price, cart shows correct quantity/price). |
| Known gap — Stage 3.5 max-reached blocking | A "multiple" group's cap-reached blocking (disabling unselected options once `maxSelections` is hit) is implemented per spec but **not on-device-verified** — no current seed data (placeholder or soup-stall) has a capped multiple-select group (`maxSelections: null` everywhere multiple selection is used). Verify this path once such data exists. |
| Ingredient photo grid | ✅ Done — `ModifierGroupSelector`'s Ingredients section (any `multiple` group whose options are `quantityEnabled`) now renders as a 2-column tap-to-select photo grid instead of the plain list: photo/first-letter-fallback, teal border + checkmark badge + quantity stepper when selected, "+ Add" button otherwise. Soup Base/Carb Base (single-select) unaffected. Verified on-device against soup-stall data (all placeholder-lettered, no real `imageUrl`s yet) — underlying min/max/quantity validation unchanged and still correct through the new interaction. |
| Cart (bottom sheet) | ✅ UI done — "Continue to payment" still a stub (logs, doesn't submit) |
| Checkout/Payment | 🔲 Not started — blocked on auth + order creation |
| Order Status (detail view) | 🔲 Not started — Home's inline card uses mock data |
| Login/OTP screens | 🔲 Not started — blocked on backend auth |
| Order history | 🔲 Not started |
| Rewards (loyalty/redemption) | 🔲 Spec'd (docs/loyalty-rewards-v1.md). Stage 1 (shared-types/api-client schemas — LoyaltyTransaction, RewardOffer, RewardRedemption) done and verified. Stage 2 (Prisma migration, point-earning trigger on order completion, /rewards/offers + /customers/me/loyalty endpoints, seed offers) and Stage 3 (Rewards tab screen) not started. |
| Account | 🔲 Stub placeholder only |
| Internationalization (English / Bahasa Malaysia / Simplified Chinese) | ✅ **Built and verified on a real device.** Built: `i18next` + `react-i18next` + `expo-localization` (device-locale default, `fallbackLng: "en"`), persisted user choice (`@react-native-async-storage/async-storage`), a language switcher on the Account tab, and every screen/component now reads its text through `t()` (77 keys in `src/i18n/locales/en.json`, incl. `ProductDetailScreen`, `CartSheetScreen`, the ingredient photo-grid cards and the order-progress status badge — the last three were found still hardcoded by an audit and converted). A key audit finds no `t()` key missing from `en.json`. **Bug found and fixed while testing key resolution:** the cart bar's item count (`cartBar.itemCount_one`/`_other`) rendered the raw key text — `i18n/index.ts` runs `compatibilityJSON: "v3"` (which looks for `_plural`), but `en.json` uses v4 `_one`/`_other` suffixes, so i18next's `count` plural lookup never matched (confirmed by an A/B against both settings). `CartBar` now picks the singular/plural key explicitly instead of relying on i18next's plural resolution (dropping v3 wasn't safe: without `Intl.PluralRules` i18next falls back to v3 handling anyway, and whether Hermes has it is unverified). Translators: for ms/zh, fill **both** `_one` and `_other` (those languages have one plural form, so the same text in both). **Translations:** real `ms.json` / `zh.json` supplied and filled in verbatim (77/77 keys, none empty). Verified: valid JSON; every `{{placeholder}}` matches `en.json` key-for-key (20 occurrences across 15 keys); `heroCard.headline`'s `\n` preserved; `cartBar` `_one`/`_other` identical within each file; `orderProgress.steps.collected` and `.completed` differ in every language (Telah Diambil / Selesai; 已取餐 / 已完成); all 231 resolutions (77 keys × 3 languages) through i18next with the app's exact options return no raw keys, no leftover `{{ }}`, and every variable substituted; and 45 exact-string checks (the 14 call shapes the components use × en/ms/zh, plus collected≠completed per language) match the values as supplied. All of this is **data/key-level verification only** — it exercises the translation library, not the rendered app. **Verified on a real device** (Samsung SM-S948B, Android 16, Expo Go / SDK 51; real screenshots plus accessibility-tree text): (1) **Malay** — text changes on the tab labels (Laman Utama · Menu · Troli · Ganjaran · Akaun), Home, Menu, Product Detail, the cart sheet, the empty-cart sheet, Account and Rewards; (2) **Simplified Chinese** — same, tabs 首页 · 菜单 · 购物车 · 奖励 · 账户; (3) **persistence** — the last-selected language survived complete force-stops (process confirmed gone, then a cold relaunch): Chinese twice, Malay once, none reverted to English; (4) **cart bar count** — English "1 item" / "2 items", Malay "1 item" / "2 item", Chinese "1 件商品" / "2 件商品" (the raw-key bug is gone); (5) **order-progress walk** Preparing → Ready → Collected on Home's card (mock temporarily changed, then restored byte-for-byte): the final step label and the status pill are different words in every language — Collected/Completed, Telah Diambil/Selesai, 已取餐/已完成; (6) the **hero headline's line break** renders correctly in both ("Kegemaran anda, / sedia bila-bila masa"; "您的最爱， / 随时为您准备好"), and no Malay or Chinese string overflowed or was clipped anywhere, including the longest buttons ("Teruskan ke pembayaran · RM17.00", "Pilih pilihan yang diperlukan"). **Cosmetic issues found on-device:** (a) **Malay stepper** — not fixed, logged as its own known gap in the row below; (b) the **Chinese hero supporting copy** left a two-character orphan ("取餐。") alone on its second line — **fixed**: `zh.json`'s `heroCard.supportingCopy` now carries an explicit line break after the em dash ("提前下单，无需排队 —" / "我们会为您准备好，随时取餐。", 11 + 14 characters), re-verified on-device. Note this is the one place zh has a hard `\n` where en/ms rely on natural wrapping — a translator adjusting that string should keep or re-place the break. (The hero *headline*, "您的最爱，/ 随时为您准备好", always wrapped fine.); (c) the **Account tab's language switcher** showed a grey band merging into the status bar — **fixed**, but note the original diagnosis ("no top safe-area inset") was wrong: `TabScreenShell` already applies the inset to every tab (and measured spacing was normal, ~20dp below the status bar vs ~27dp on Home). The real cause was that the switcher had no background, so the navigator's default `#F2F2F2` (identical to the Android status bar) showed through while Home/Menu paint their own cream; `LanguageSwitcher` now sets the cream page background (measured `#F2F2F2` → `#FBF8F1`). Adding `useSafeAreaInsets` here would have double-applied the inset on iOS; (d) Chinese labels sit a few px lower than Latin text on the same row (e.g. "总计" vs "RM17.00") — not fixed; (e) the React Native dev warning toast (`fontFamily "Inter"`) covers the bottom bar/buttons in dev builds only. **Not verified:** whether English flashes for a moment on a cold start before the stored language is applied (the code applies it just after first paint; too short to catch over adb); iOS. Testing note: tapping the Cart tab opens the (empty) cart sheet over the screen, so further taps must close it first. Deliberately left out of scope: server-supplied content (product/outlet names, descriptions, categories) is English data from the backend and is not localized, and the "HEY FOOD" wordmark is the brand name. Committed as one commit (infrastructure + switcher + all three languages + the two converted screens) after being device-verified across all screens. |
| Known cosmetic gap — order-progress stepper with long labels (seen in Malay) | ⚠️ **Known gap, deliberately NOT fixed; low priority, and more involved than one string.** **What:** on Home's "Order in progress" card, each step is a column sized by its label, and the connector lines between the circles take whatever width is left. With Malay's "Sedang Disediakan" (the longest label) the widest column crowds the row: the connectors between the first three circles shrink to short dashes and adjacent labels almost touch ("Dibayar Diterima Sedang Disediakan Sedia Telah Diambil" on one line). Nothing overflows or wraps, and English and Chinese (short labels) keep full connector lines — verified on a real phone in portrait. **Why it's not a one-string fix:** shortening this one translation would only hide it; the stepper itself has no policy for variable-width labels, so any future language or longer label brings it back. **Likely proper fix (not started):** give the columns equal fixed widths with wrapping/ellipsizing labels, or anchor circles and connectors independently of label width. **Where to see it:** switch to Bahasa Malaysia and look at Home's order-progress card (the mock order is `preparing`, so the current step reads "Sedang Disediakan"); the `Ready` and `Collected` states look the same way. |

## Outlet POS (apps/hey-food-pos)

| Screen/Feature | Status |
|---|---|
| Login | ✅ **Stub only** — one button, no PIN pad, no device-outlet-binding check. docs/hey-food-developer-spec-v1.md Section 5.5 describes real login as PIN-based with the device pre-bound to its outlet at setup; neither exists yet. Placeholder is flagged both in code comments and with an on-screen caption ("Demo login — real staff PIN auth not yet built") — the one stub this session where surfacing the placeholder on-screen, not just in source, felt warranted. |
| Order Queue (docs/hey-food-developer-spec-v1.md Section 5.1) | ✅ First real screen built — three columns (New/Preparing/Ready) mapped to `received`/`preparing`/`ready` OrderStatus values, order cards (ID, item count, timestamp, status badge, stage-appropriate action button per docs/hey-food-design-system-v1.md Section 6), tapping Start/Ready/Collect transitions the order through `PosOrderStatus` locally. Verified on-device (phone, not a tablet — see limitation below). **Stub data**: mock/orders.ts, no live order feed — `POST /orders` doesn't exist yet (blocked on auth), so there's nothing real to subscribe to. Section 5.1's real-time (websocket/poll) update mechanism, mandatory new-order sound+visual alert, and offline action-queueing are all out of scope — explicitly not simulated, not just deferred silently. |
| Live order feed + staff actions — **Stage A (read) and Stage B (write)** | ✅ **Built and device-tested; the queue reads real orders AND Start / Ready / Collect / Cancel now persist to the server.** Polls `GET /pos/outlets/:outletId/orders` every 5 s (`src/orders/useLiveOrders.ts`); `EXPO_PUBLIC_POS_USE_MOCK_ORDERS=1` restores the mock queue (mock actions stay local). **Actions are optimistic:** the card moves on tap, the change is sent (`PATCH …/status` / `POST …/cancel`), and the server's copy replaces the guess; **if the save fails for any reason the change is rolled back and a red "Couldn't start #PM003: … It has been put back as it was." banner says why** (tap to dismiss, or 10 s). One action per order in flight (a second tap is ignored); a poll that started before a save landed is discarded so it can't flick the card back. The old "kept on this tablet only" notice is **gone** — a healthy live queue shows no caveat; the notice strip now appears only when the feed is OFFLINE, in mock/demo mode, or while loading. The top-bar indicator (ONLINE / OFFLINE / CONNECTING / DEMO DATA) is real. **Verified on a real phone (Samsung, Android 16, Expo Go) with screenshots and DB reads, one order placed through the real guest web checkout in Chrome:** Start → `preparing_at` in the DB and the guest's open browser page changed to "Preparing" by itself; Mark Ready & Call Customer (from Order Detail) → `ready_at`, guest page "Ready for pickup"; Collect → `collected_at` = `completed_at`, guest page "Collected", app back on the queue; Cancel with reason Other + typed "Ran out of sambal" → `cancel_reason = other`, `cancel_reason_detail` stored, the guest's own view shows it cancelled; **rollback on a 409** (the order was cancelled elsewhere, then Start tapped before the next poll → card restored, banner quoted the server's reason); **rollback offline** (backend stopped, Start tapped → card restored, "can't reach the server" banner) and the same tap **succeeded and persisted** once the backend was back. **Still missing:** (a) **real customer notification on `ready`** — the server now fires it (see the "Ready notification" row) but only to logging stubs, so nobody is actually notified; the Order Detail caption "the customer is not actually notified yet" is kept and is still true. **The next real gaps are a real SMS provider and, for push, the token registry + Customer App push code**; (b) **offline action queue** (dev spec 5.1 wants offline taps held and replayed; today they're undone with an error, never silent but lost); (c) websocket feed and new-order sound/visual alert; (d) **refunds** on cancelling a paid order; (e) **real staff/device auth** (see the stand-ins row — the key now guards writes). |
| Known limitation — phone testing | Tested on the same phone used throughout this session, not a real Android tablet. Columns are sized (`min 300px` each) to fit three side-by-side on a typical tablet in landscape with no scrolling; on the phone only about one column is visible at a time and the queue must be scrolled horizontally. Noted as a reasonable stand-in per explicit instruction, not fixed — real tablet testing is future work. |
| Menu Availability (docs/hey-food-developer-spec-v1.md Section 5.3) | ✅ Built — list of outlet products, each with a single toggle button whose label/color IS the current state ("AVAILABLE" teal / "SOLD OUT" ink), tap flips it. No price-editing affordance at all, by design — Section 5.3 is explicit that staff can only ever touch availability, never price, even though the underlying `OutletProductOverride` type also carries `priceOverride`. **Stub data**: mock/products.ts (one override row per product; Fried Chicken starts sold out, matching seed-placeholder.ts's own starting state) — no `PATCH /pos/outlets/:id/products/:productId/availability` endpoint exists on the backend yet. Verified on-device: toggle flips state and survives navigating to Queue/Summary and back. |
| Daily Summary (docs/hey-food-developer-spec-v1.md Section 5.4) | ✅ Built — 4 stat tiles (sales today, orders, avg prep time, cancelled) + a "Top selling today" list. **Fully static placeholder numbers**, flagged with an on-screen "PLACEHOLDER DATA" banner (same reasoning as Login's visible stub caption — these look like authoritative business metrics, and staff glancing at the screen has no other way to tell they're fake). Section 5.4 requires these aggregates be computed server-side so POS and HQ never disagree on the same number; no such aggregation endpoint exists yet, so unlike Queue/Menu Availability's mock data, there's no real shared-types/api-client shape this validates against either — nothing to validate against yet. |
| Order Detail (docs/hey-food-developer-spec-v1.md Section 5.2) | 🟡 **Built and device-tested; one layout issue open (see the known-gap row below).** Tapping a queue card (the inline Start/Ready/Collect button still works, and doesn't also open the detail) opens a full-screen detail view inside the Queue tab; Back / the top-bar Queue button / Android hardware Back return to the unchanged three-column queue. Shows: order ID at 1.5× the card's display size, status badge, item count + placed time; every line item with quantity and **every selected modifier**; one primary action for the current status; and Cancel. **Verified without a device:** typecheck + lint clean, isolated and workspace-wide; 40 logic checks on the pure code underneath it (mock-data schema validity and hand-checked subtotals, modifier grouping incl. the longest 4-ingredient case, every status transition and its timestamp, the full Start→Ready→Collect chain, all four cancel reasons, and rejection of invalid/non-staff cancel requests by the real api-client schema). **Verified on a real device** (Samsung SM-S948B, Android 16, Expo Go / SDK 51, driven over adb with real screenshots, accessibility-tree reads and pixel sampling): (1) tapping a queue card opens the detail and shows the full item + modifier list (PM101: Chicken Rice with Spice Level / two Add-ons / Remove, plus a plain Iced Tea line); (2) the two-bowl order PM103 — four ingredients at ×2/×1/×3/×1 render one per line with no wrapping (longest name "Mixed Vegetables" fits), the bold ×2/×3 vs muted ×1 distinction reads clearly, no CARB BASE block when none was picked, and the customer note shows in its own labelled box; (3) "Applies to each of the N" appears for PM105 (2× DIY Soup Bowl) and PM104 (2× Chicken Rice) and correctly does NOT appear for quantity-1 items or plain items; (4) Cancel: all four reasons select exclusively, the free-text field appears **only** for Other, the confirm button is disabled until a reason is picked, and **the danger colors match the token exactly at the pixel level** (header button `#FEE2E2` fill / `#991B1B` text; dialog confirm `#991B1B`; selected row `#FEE2E2` with `#991B1B` border and radio; disabled state = `#991B1B` at 40% opacity); (5) Collect from the detail returns to the queue and the order leaves READY (READY (0)), and confirming a Cancel returns to the queue and the order leaves its column (NEW 2→1); (6) hardware Back on the detail returns to the queue, and with the cancel dialog open the first Back closes only the dialog (the order is not cancelled) and the second returns to the queue; the top-bar QUEUE button also returns from the detail. The side-by-side modifier-label layout (≥700dp) was also verified rendered, by rotating the phone to landscape at an emulated 1170×540dp tablet viewport. **Bug found and fixed on-device:** with the keyboard up, the Other text field and both dialog buttons were hidden behind it (a Modal is its own Android window, which doesn't resize for the keyboard, and `KeyboardAvoidingView` doesn't help there). Fixed in `CancelOrderModal`: backdrop padding tracks the real keyboard height, the Keep/Cancel buttons sit outside the scrolling area, the list scrolls to the field, and a top safe-area inset stops the dialog running under the status bar (which the first fix introduced). Re-verified on-device: typing is visible and Cancel works with the keyboard still up. **Still open / not verified:** (a) **landscape / short viewports — a known layout gap, logged as its own entry in the row directly below.** (b) On the phone in portrait, "MARK READY & CALL CUSTOMER" wraps onto two lines and the button + caption take ~14% of the height. (c) With the keyboard up, Android Back closed the whole dialog and discarded the typed text in one observation (not re-tested after the fix). (d) The React Native dev warning toast (`fontFamily "Inter"`, the known Inter gap) sits on top of the footer button in dev builds only. (e) Not tried on a real tablet or in a release build; Back pressed on the queue itself (app exit behavior) not tested. (f) Testing note: `adb input text` while no text field is focused triggers React Native's double-"R" dev reload — check the keyboard is up before typing. **Data source (updated by Stage B):** it was built against mock data with local-only actions; the screen now shows real orders and its Mark Ready / Collect / Cancel are saved to the server (`PATCH /pos/orders/:id/status`, `POST /pos/orders/:id/cancel`), with optimistic update and rollback — see the "Live order feed + staff actions" row. Mark Ready notifies no customer for real (only logging stubs exist), hence the on-screen caption. **Mock data:** `mock/orders.ts` now has real modifiers on 4 of 5 orders (PM102 deliberately plain), with option names/prices from the seed files. Two of those orders use *DIY Soup Bowl*, which is soup-stall seed data, not Paradigm Mall's — the mock outlet doesn't sell it in either seed; accepted because it's the only seeded product with per-option quantities. **Judgment calls:** (1) the Preparing→Ready button reads **"Mark Ready & Call Customer"** on the detail screen (per spec 5.2) but **"Ready"** on the compact card — same transition; and it does not actually notify anyone (the server fires the ready notification, but only to logging stubs; see the "Ready notification" row), so the screen carries an on-screen "Demo build: the customer is not actually notified yet" caption under it. (2) Modifier layout: grouped by modifier group, **one option per line** with its quantity in a fixed left gutter (bold for ×2 and up, muted for ×1 — always shown so "no number" is never ambiguous) instead of a run-on "Fish Balls x2 · Tofu x1" sentence; group labels sit beside the options at ≥700px width and above them below that; the customer note gets its own labelled block; and when an item's quantity is above 1, an "Applies to each of the N" line spells out that modifiers are per unit (they belong to the one line item — "Fish Balls ×2" on "2× DIY Soup Bowl" is two per bowl). (3) Cancel sits top-right, far from the primary action, styled as a tinted Secondary button; the destructive filled button exists only inside the required confirmation dialog. Both use the new `DANGER_COLORS` token (`solid` `#991B1B` / `tint` `#FEE2E2`, see design-tokens below) — deliberately an independent token, not a reference to the Cancelled status color, even though the values match today. (4) "Other" with no text is allowed (`otherDetail` is optional in api-client); blocking a mid-rush cancel on a missing sentence seemed worse. (5) After **Collect** or **Cancel** the app returns to the queue on its own (the order has left it); **Start** and **Ready** keep staff on the detail, where the button advances to the next step. (6) The status-transition logic now lives once in `src/orders/transitions.ts`, shared by the queue cards and the detail (the queue's inline copy was removed) so the two can't drift. **Spec gap found and resolved:** `Order.cancelReason` was a single string while the cancel request carries `reason` and `otherDetail` separately, with nothing saying how the server stores both. Fixed by adding a nullable `Order.cancelReasonDetail` (see the Backend table): `cancelReason` is now always the clean enum value, `cancelReasonDetail` holds the free text only for `other`, and the POS mock's cancel now produces that real shape instead of a composed `"other: <detail>"` string (blank/whitespace-only detail is stored as null). Not modelled: refunds for cancelled paid orders (spec Section 3), and there's no view of cancelled orders (they simply leave the queue). |
| Known layout gap — Order Detail on landscape / short viewports | ⚠️ **Known gap, deliberately NOT fixed in this pass; did not block committing the Order Detail work.** **What:** on a landscape or otherwise short screen, the fixed chrome on the Order Detail screen leaves almost no room for the thing the screen exists to show — the item + modifier list. What competes for height: the persistent top bar (outlet name + the three nav buttons, ~155dp), the Back/Cancel row, the very large order-ID card (the ID is 1.5× the queue card's display size, inside a padded card), and the fixed footer (a tall primary button plus, on Preparing orders, the "Demo build" caption, ~100dp). **Measured on the real test phone** (approximate, from screenshots): in landscape at the phone's normal density the screen is 384dp tall and the scrolling list gets roughly **26dp** — effectively nothing; emulating a tablet-sized 1170×540dp landscape viewport (by lowering the phone's display density) it opens showing only the "ITEMS (3)" label, because the Back/Cancel row and ID card use ~200dp of the ~250dp scroll area, and once scrolled only about two modifier groups are visible at a time. **Not affected:** portrait (the phone-portrait layout was verified and reads well), the landscape queue (the three-column queue renders properly in landscape), and the side-by-side modifier labels (verified rendered in landscape). **Why it may matter later:** POS is specified as an Android tablet (dev spec Section 5.1 / 5.5) and a tablet in a stand on a food-stall counter typically runs landscape, so this is worth taking seriously — but it is **undecided whether POS tablets will actually be used in landscape in practice**, and that answer should drive whether and how to fix it. **Decision needed first:** how will the POS tablets be mounted and oriented at the real stalls? **Options to weigh once that's known** (none started): a two-pane landscape layout (order ID / status / actions in a left column, the item list filling a right column — uses the wide screen instead of stacking); a more compact header (ID, badge and meta on one row, smaller ID); hiding the top bar's nav row while a detail is open (the Back button already exists); making the footer non-sticky, or shorter, when the viewport height is small. **To reproduce:** rotate the device to landscape and open any order; to emulate a tablet-sized viewport on a phone, `adb shell wm density 320` with the phone in landscape, and restore afterwards with `adb shell wm density reset` (the app resets to the login screen when the density changes). |
| Navigation (Queue/Menu/Summary) | ✅ Top bar with 3 large nav buttons, always visible on all three screens — judgment call over a menu icon/drawer, since POS's whole ethos is "minimal decoration, large touch targets" and hiding 3 destinations behind an extra tap has no real benefit here. Queue's `orders` and Menu Availability's `overrides` are both owned by a new `PosShell` component (lifted out of each screen) specifically so switching screens doesn't reset either one's state. |
| Follow-up backend dependency | Two endpoints from dev spec Section 5 don't exist yet and are what would replace these screens' mock data: `PATCH /pos/outlets/:id/products/:productId/availability` (Menu Availability) and a server-side daily-aggregates endpoint (Daily Summary, Section 5.4). |

## HQ Admin (apps/hey-food-hq)

| Screen/Feature | Status |
|---|---|
| Login | ✅ **Stub only** — no real HQ Admin auth exists yet (blueprint Section 6 lists role-based login as screen 1 of 12; role-based-ness itself isn't built). A plain Next.js `<Link>` to /dashboard, no session created anywhere. Same visible on-screen caption treatment as Outlet POS's login ("Demo login — real HQ Admin auth not yet built"). |
| Dashboard (dev spec Section 9.1, blueprint Section 6) | ✅ First real screens built — today's aggregate stat cards (sales, orders, avg order value, % vs yesterday), an outlet performance list, and the 3-state outlet health indicator (good/warning/critical dots + label). Layout/content structure follows the blueprint mockup; **colors do not** — that mockup used the old Ember/Char palette, which no longer exists in design-tokens after the Customer App pivot, so every color here comes from the current design-tokens instead. **Stub data**: mock/dashboard.ts, using the 3 real seeded outlets (Paradigm Mall/KSL City/Mid Valley) with invented sales/orders/health numbers — no aggregation endpoint or scheduled health-computation job exists yet (Section 9.1 requires both). Flagged with an on-screen "PLACEHOLDER DATA" banner, same reasoning as POS's Daily Summary. Verified in a real browser (Chrome via `pnpm dev`), not just by reading the code. |
| New design-tokens: `OUTLET_HEALTH_COLORS` | ✅ Added and documented in docs/hey-food-design-system-v1.md Section 2, same treatment as `ORDER_STATUS_META`'s own table — **a genuine proposal, not a values lookup**. No 3-state status-color system existed for outlet health before this. `good` reuses `BRAND_COLORS.teal`. `warning` (`#D97706`) is a **brand-new color, deliberately not** `BRAND_COLORS.yellow` (`#F5C451`) — yellow is reserved for the single highest-emphasis CTA per screen, so reusing it for a status dot would be a real conflict, not just a style note, and the two hexes are chosen to read as visually distinct at a glance. `critical` (`#DC2626`) is also brand-new — no red exists anywhere else in the palette. Both `warning` and `critical` are proposed values, flagged for designer confirmation before being treated as final. |
| Navigation shell | ✅ Left sidebar, not POS's top-button-row — chosen because HQ will eventually have ~12 top-level screens (blueprint Section 6) vs. POS's 3, and a button row doesn't scale past a handful before wrapping/crowding. Sidebar lists the full 12-screen set from the blueprint; only Dashboard and Outlets are real links today, the rest render as disabled "soon" rows so the shell visibly demonstrates it scales without needing 10 fake screens built to prove it. |
| Outlet List (/outlets, dev spec Section 6/9.2) | ✅ Built — every outlet (name, open/closed status, today's sales/orders, health indicator), each row clicking through to that outlet's Detail screen. Same "fake table via flex rows" pattern as Dashboard's existing outlet lists, not a literal `<table>`. **Stub data**: mock/outlets.ts, using the 3 real seeded outlets — Dashboard's own outlet-performance mock data was consolidated into this same file so the two screens can't silently disagree about the same outlet. Verified in a real browser: List → Detail → back via breadcrumb all confirmed working, plus Dashboard's own outlet rows now link into the same Detail screen. |
| Outlet Detail (/outlets/[id], dev spec Section 9.2) | ✅ Built — name/address/status, a read-only "live queue" snapshot (New/Preparing/Ready counts), and today's stats (sales, orders, avg prep time, cancelled, refunds). One outlet (Mid Valley) is hardcoded into a "POS offline" state to demonstrate Section 9.2's "connection-status warning banner" — that banner's exact copy/layout is my own construction, not a reproduction of any existing mockup (the spec only describes it in one sentence, no design exists for it anywhere in the docs). A shared `HealthDot` component was extracted once a third screen needed the same health-dot rendering Dashboard already had. |
| Menu Management, Orders, Staff, Customers, Payments, Reports, Settings | 🔲 Not started |

## Guest Web Checkout (apps/hey-food-web)

A lightweight, browser-based ordering entry point for customers without the app: scan an outlet's QR → web menu → customize → phone number only (no account/login/OTP) → Billplz → normal `Order` in the outlet's POS queue → "ready" SMS. **Additive and parallel** to the app flow: `POST /orders` stays strictly "authenticated customer required" and gains nothing; guests get their own `POST /guest/orders` that will share the same internal order-creation service once it exists. Design reviewed and approved before build (5 judgment calls + rate limiting, E.164 normalization, scoped CORS allow-list).

| Stage | Status |
|---|---|
| 1. Schema — `orders.customer_id` nullable, `guest_phone`, `guest_token_hash`, `orders_identity_xor` CHECK | ✅ Done. Migration `20260919055107_add_guest_order_identity`, applied to the dev DB. CHECK is hand-written in the migration SQL (Prisma can't express CHECK constraints): an order is exactly one of app-based (`customer_id` set, no guest fields) or guest (`customer_id` null, phone AND token hash both set). **Verified against the live DB**: app order and guest order accepted; neither / both / phone-without-hash / stray-hash all rejected (Postgres 23514); deleting a customer who has orders still blocked (23001). |
| 2. api-client — `CreateGuestOrderRequestSchema` / `CreateGuestOrderResponseSchema`, phone normalizer | ✅ Done. Request is built with `CreateOrderRequestSchema.extend({ guestPhone })` so the item shape (and the server modifier validation behind it) cannot drift from the app flow. `guestPhone` parses to canonical E.164 via `normalizeMalaysianMobile` (Malaysia mobile only, accepts `012-345 6789` / `60…` / `+60…`; rejects landlines, other countries, junk). Response = `OrderWithItems` + `guestToken` (returned once; only its hash is stored). `Order.customerId` is now `string \| null` in shared-types and `OrderSchema` — **null means guest**, that's the whole discriminator. `guestPhone`/`guestTokenHash` deliberately NOT on the shared `Order` shape (the hash must never leave the server). |
| 3. Shared selection rules — `shared-types/src/modifier-rules.ts` | ✅ Done. `countSelected`, `isGroupSatisfied`, `isAtMax`, `areAllGroupsSatisfied`, `setOptionQuantity` (radio exclusivity). **Verified by a 20,000-case randomized parity test against the real server `validateSelectedModifiers`** (incl. capped groups): client and server agreed on every case. |
| 4. Web app — `/o/[outletId]` menu, `/o/[outletId]/item/[productId]` customizer | ✅ Done. Next.js 14 (same stack/tokens as HQ, port 3002), server components fetch `GET /outlets/:id` live (`no-store`; availability changes at the POS). Verified in a real browser against the real backend + seeded data. Add-to-cart builds a client-side cart; the cart bar's **Checkout** now leads to the checkout page (Stage 5). |
| 5. `POST /guest/orders`, shared order-creation service, rate limiting, scoped CORS allow-list, guest-token order/pay access, web checkout | ✅ **Done and verified end to end in a real browser** (payment **stubbed**, see the stand-ins row under Backend). Flow: cart → **Checkout** (`/o/[outletId]/checkout`: cart summary, phone with inline E.164 validation, "Continue to payment") → `POST /guest/orders` (order `pending`, token to `sessionStorage`, cart cleared) → **order page** (`/o/[outletId]/order/[orderId]`: the SERVER's real total incl. service fee, then **Pay RM…**) → stub pay → status polled every 5 s. Judgment call: order creation and payment are two steps so the guest sees the true total before paying (the real Billplz flow has the same shape). **Verified in Chrome:** landline number blocked inline; order PM001 created (CORS preflight 204 → POST 201); RM25.00 + RM2.00 fee = RM27.00 shown; DB row correct (`customer_id` NULL, phone `+60123456789`, 64-hex token hash, item + 3 modifier snapshots, no `Payment` row); pending order absent from the POS queue, present after pay as `paid`; "TEST MODE" banner + status "Order received"; page survives a reload; the same URL in a fresh tab (no token) shows a safe "can't show this order here" page; `[STUB PAYMENT]` logged. The order page is browser-bound by design — losing the tab loses the page, not the order. |
| 6. Billplz (real `POST /orders/:id/pay` + webhook), SMS notifier ("ready" via SMS as the guest's PRIMARY channel), post-collection "Download the app" prompt | 🔲 **Not started.** The pay endpoint and the web app's pay button already exist against the stub with the final contract (`{ redirectUrl }`), so Billplz replaces one call. **The "ready" SMS is only a logging stub** — the checkout copy still deliberately promises only "identifies your order at the counter", not a text message, because nothing is actually texted yet. The `SmsProvider` interface and a logging stub now exist and the guest's "ready" SMS is fired through them (see the "Ready notification" row), but the stub sends nothing and **no real SMS provider is chosen** (auth's OTP needs one too — decide once), so a guest is still never actually texted. |

**Notes / judgment calls from this pass:**
- **Duplicated rule logic — tracked, optional future cleanup, not urgent.** The Customer App still carries its own inline copies of the selection rules (`ModifierGroupSelector.tsx`, `ProductDetailScreen.tsx`'s `isValid`) — deliberately left untouched per the "don't touch the app flow" constraint. The web app uses the shared module instead. Server-side validation is the authority for both, so drift can only cause a UX mismatch, never a bad order. Migrating the Customer App to the shared functions is a small, isolated refactor whenever convenient. One intentional difference to preserve: the shared `isGroupSatisfied` is the *validity* rule (optional group with nothing selected is satisfied); the app's teal "satisfied" styling additionally requires count > 0, purely visual.
- **`customer` FK stays `ON DELETE RESTRICT`.** Prisma silently changed it to `SET NULL` when the relation became optional, which would orphan an order into neither identity and violate the CHECK. Pinned explicitly with `onDelete: Restrict`.
- **Real bug caught by browser testing, fixed:** the cap-reached locking (`isAtMax`) initially also locked unselected options in *single-select* groups, making it impossible to switch e.g. Spice Level once one was picked. Locking now applies to `multiple` groups only (a radio's cap is 1 by definition; switching is handled by `setOptionQuantity`). Note this is the same cap-locking path the Customer App's "Known gap — Stage 3.5 max-reached blocking" row says has never been verified on-device; the parity fuzz now exercises capped *multiple* groups against the server's validator, but there is still no seeded capped multiple-select group to click through in a UI.
- **CORS is now on** (Stage 5): the browser calls the backend directly for order create / read / pay. Allow-list = exactly `WEB_ORIGIN` (default `http://localhost:3002`), methods GET/POST, headers `Content-Type`/`Authorization`/`Idempotency-Key`; verified a foreign origin gets no `Access-Control-Allow-Origin`. The web app reads `NEXT_PUBLIC_API_BASE_URL` (default `http://localhost:3000`); menu pages still fetch server-side via `API_BASE_URL`.
- **Ingredient photo grid not replicated on web** — ingredient options render as list rows with steppers (every `imageUrl` is null today anyway). Same rules, simpler presentation.
- **Not runtime-verified:** the "outlet closed" path (banner + disabled add-to-cart) — every seeded outlet is open, and flipping one closed would mean editing the user's dev data. Reviewed by code only.
- **Testing per-option quantity / `min 2` needs the soup-stall seed** (`outlet_soup_stall_main`, product `prod_diy_soup_bowl`) — the placeholder profile has no `quantityEnabled` options. Both were exercised in a real browser this pass, then the dev DB was reset (`migrate reset`) back to the placeholder profile only (the README's default), so the web app's soup-stall URLs 404 until that seed is run again. Also confirmed: all 5 migrations (incl. the new one) apply cleanly to an empty database.
- **Cart `sessionStorage` persistence verified** across a reload with no hydration warnings (cart is a `useSyncExternalStore`-backed store, keyed per outlet). The stored cart contains display fields + option IDs only.

## Known environment quirks (for future reference)

- **Backend Prisma/tsx commands fail from `pnpm run` in PowerShell — `scripts/with-x64-node.mjs` wipes `PATH`. Root cause verified, workaround below, wrapper deliberately not fixed yet.**
  - **Symptom:** `pnpm run prisma:migrate`, `prisma:generate`, `prisma:seed`, `db:seed:*` (anything whose script is `node scripts/with-x64-node.mjs <bare-command> …`) die with `'prisma' is not recognized as an internal or external command` (or `'tsx' is not recognized…`). `pnpm run dev` / `start` are unaffected — their bare command is `node`, which the wrapper itself supplies.
  - **Cause (measured, not guessed):** in PowerShell the parent env key is `Path` (mixed case). The wrapper does `const env = { ...process.env }` — a plain, case-*sensitive* copy — then ``env.PATH = `${x64NodeDir};${env.PATH ?? ""}` ``. `env.PATH` is `undefined` on that copy, so the child's `PATH` becomes **only** the x64 Node dir (probe: 19 entries direct → 1 entry via the wrapper). That drops pnpm's injected `node_modules/.bin` *and* the whole rest of the system PATH. It likely works in shells where the key is exactly `PATH` (e.g. Git Bash) — which would explain why it worked in earlier sessions — but that part is untested.
  - **Workaround (works; use these):** invoke the tool's JS entrypoint directly under the same wrapper, from `apps/hey-food-backend`:
    - Prisma: `node scripts/with-x64-node.mjs node node_modules/prisma/build/index.js <args>` — e.g. `migrate dev`, `migrate dev --create-only --name x`, `generate`, `migrate reset --force --skip-seed`.
    - Seeds/scripts: `node scripts/with-x64-node.mjs node node_modules/tsx/dist/cli.mjs prisma/seed-placeholder.ts` (or `seed-soup-stall.ts`).
    - **`--skip-seed` on `migrate reset`** — Prisma's built-in seed step shells out to bare `tsx` (same PATH problem) and will fail; run the seed explicitly as above afterwards.
  - **`pnpm` itself isn't on PATH in these shells** — use `corepack pnpm …` (packageManager is pinned to pnpm@9.12.0). Workspace-wide `corepack pnpm -r run typecheck|lint` works fine.
  - **The proper fix (not done — wrapper was out of scope):** read the base PATH case-insensitively *before* spreading, e.g. `const basePath = process.env.PATH ?? ""` (Node's `process.env` proxy is case-insensitive on Windows) and set ``env.PATH = `${x64NodeDir};${basePath}` ``, deleting any differently-cased `Path` key from the copy. One line; then the `prisma:*`/`db:seed:*` scripts work through pnpm again and the workaround above is unnecessary.
- **`prisma generate` (and `migrate dev`/`reset`, which run it) fails with `EPERM … query_engine-windows.dll.node` while a backend dev server is running** — the running server holds the engine DLL. Stop the `with-x64-node … node --watch` backend process tree (`taskkill /PID <wrapper pid> /T /F`; there may be more than one duplicate tree), generate/migrate, then restart with `corepack pnpm run dev` from `apps/hey-food-backend`. The migration itself still applies before the generate step fails, so a failed generate leaves the DB migrated but the client stale.
- **Metro servers left running for days go stale — restart with `--clear` before a device test.** A week-old POS Metro (:8082) failed to bundle with "Unable to resolve module expo-constants" even though the package resolves fine; a fresh server built the identical code with no error, so it was stale server state (`node_modules` had been re-linked under it), not a broken install. Start each app's Metro from its own directory: `corepack pnpm exec expo start --clear` (Customer, :8081) and `corepack pnpm exec expo start --port 8082 --clear` (POS). Device testing also needs `adb reverse` for 8081/8082 (whichever app) and 3000 (backend).
- **A POS Metro started with `CI=1` never picks up file changes.** CI mode disables file watching, so after code changes the phone kept loading the old bundle (this pass it still showed the old "kept on this tablet only" notice). Start it plain (`corepack pnpm exec expo start --port 8082 --clear`) and confirm the bundle contains a new string before testing on a device.
- **`pnpm run dev` (`node --watch`) restarts the backend on its own mid-request.** Node's watcher treats lazily-loaded `node_modules` files (multer, iconv-lite…) as changes the first time a route needs them, so the server bounced during the first API test (`ECONNRESET`). For testing use `corepack pnpm run start` (same code, no watcher). Also: killing the wrapper PID leaves the `--watch` parent alive, which respawns the server and later hits `EADDRINUSE` — kill every `node … src/main.ts` process (see `Get-CimInstance Win32_Process`), not just the listener.
- **`corepack pnpm run typecheck` / `lint` (Turbo) fail with "Unable to find package manager binary"** — turbo can't resolve pnpm through corepack here. Run the same scripts recursively instead: `corepack pnpm -r --no-bail run typecheck` / `… run lint`.
- **Chrome-driven testing:** coordinate clicks land wrongly (the screenshot frame is not the 1800×961 viewport); click by element ref (`find`/`read_page`) instead.
- **Checking the Customer bundle by hand needs `transform.routerRoot`.** Expo Router only finds this app's routes if the bundle URL carries `&transform.routerRoot=src%2Fapp` (the app keeps its routes in `src/app`, not `app`); without it the request still returns HTTP 200 but bundles an *empty* route tree — no screens, no i18n, nothing — which looks like success. Real devices send the parameter automatically; only hand-built `curl` checks need it. Full URL: `http://localhost:8081/node_modules/expo-router/entry.bundle?platform=android&dev=true&transform.routerRoot=src%2Fapp` (POS: `/index.bundle?platform=android&dev=true`).

- This machine runs **Windows on ARM** — Prisma has no native ARM64 query engine. Backend requires x64 Node via a wrapper script (`scripts/with-x64-node.mjs`) — documented in the backend README.
- Native device testing requires **USB + `adb reverse`** (WiFi/hotspot connections were unreliable on this network — likely router/carrier client isolation). Ports needed: 8081 (Metro), 3000 (backend).
- Project is on **Expo SDK 51** (current latest is SDK 57 as of this session) — a real gap, not deliberately chosen. Upgrade deferred intentionally until the app is further along, to avoid compounding two hard problems at once.
- Web preview (`expo start --web`) is broken by a Windows + pnpm-workspace-specific Metro bug (malformed backslash path) — deferred, not fixed. Native (device) testing is unaffected.
- Every pushed stack route (Product Detail today; future ones like Checkout/Payment/Order Status) sits outside `TabScreenShell`'s subtree and needs its own `useSafeAreaInsets()` top-inset handling — missing it means the header renders under the status bar (a real bug caught on-device while building Product Detail). Deliberately not centralized into a shared wrapper yet — see the comment in `app/_layout.tsx` for why. Don't forget this on the next pushed screen.
- **Inter font is never actually loaded** — confirmed during Stage 3.5, not just a dev-time LogBox nuisance. `FONT_FAMILY = "Inter"` (design-tokens) is applied via `fontFamily: FONT_FAMILY` everywhere, but no app calls `useFonts`/`Font.loadAsync`, no `@expo-google-fonts/*` package is installed, `expo-font` only appears transitively (via `@expo/vector-icons`, for icon glyphs, not text), and no `.ttf` asset exists anywhere in the repo. Every screen in every app has been silently rendering in the platform default system font this whole time. Real design-system gap — needs its own follow-up task to actually load Inter, not folded into this pass.
- **hey-food-hq's Tailwind dev server silently drops custom color classes** (`bg-health-*`, etc.) after most file edits, rendering them fully transparent with no error — reproduced repeatedly while building the Outlet List/Detail screens. Classes used only via a shared/extracted component (e.g. `HealthDot.tsx`) seem especially prone to this; a plain `pnpm dev` Fast Refresh does not reliably pick them back up. **Workaround**: kill the dev server, `rm -rf .next`, and restart — confirmed to fix it every time it's been hit so far. If you see a design-tokens color silently render as transparent/invisible in this app, try this before assuming the code is wrong.

## Future ideas (not in scope)

Captured for later planning only. Nothing here is scheduled, designed, or committed to. Each entry says what it is, why it matters, and what has to exist first. (The larger multi-tenant platform idea has its own document: [docs/future-platform-idea.md](future-platform-idea.md).)

### Automatic sold-out from real inventory counts

- **What:** Replace the Menu Availability screen's manual on/off toggle (dev spec Section 5.3, which writes `OutletProductOverride.is_available`) with availability driven by real stock counts, so an item goes sold-out on its own when it runs out instead of relying on staff to flip it.
- **Why it matters:** the manual toggle only works if someone notices and acts mid-rush. A missed toggle means customers can order, and pay for, food the outlet can no longer make.
- **Depends on:** inventory tracking, which does not exist yet: stock quantities per ingredient/product, decremented on every order (plus a way to restock and correct counts). That is its own prerequisite feature, not a small add-on to the toggle. Ingredient-level options that carry a quantity (e.g. "Fish Balls x2" on the DIY Soup Bowl) would presumably need to decrement stock too, so the decrement is not per product alone.
- **Source:** Eats365 POS competitive research.

### Three-tier staff roles: store / brand / organization

- **What:** A more granular access hierarchy with store, brand, and organization levels.
- **Where we are today:** three roles inside a single business: `hq_admin` (everything), `area_manager` (a defined set of outlets), and `outlet_staff` (one outlet) (blueprint Section 13; `StaffRole` in `packages/shared-types`). The proposal adds scope levels above a single business: brand and organization.
- **Why it matters, and when:** only relevant if Hey Food ever manages multiple brands or franchisees under one platform. It is not needed for the current single-business, single-brand system.
- **Depends on:** the deferred multi-tenant platform idea ([docs/future-platform-idea.md](future-platform-idea.md)), which already anticipates a platform-admin layer above HQ Admin. Brand and organization roles only make sense once `Business` can represent many independent owners.
- **Source:** Eats365 POS competitive research.
