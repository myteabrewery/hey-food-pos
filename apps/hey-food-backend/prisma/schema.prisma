// Prisma schema placeholder — Hey Food Backend.
// Data model (Business, Outlet, Order, etc.) per docs/hey-food-developer-spec-v1.md
// Section 1 is not yet implemented.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
